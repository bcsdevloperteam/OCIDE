<Page x:Class="OCIDE.Pages.ExtensionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="ExtensionsPage">

    <Border Background="#252526" Width="280" HorizontalAlignment="Left" CornerRadius="8">
        <Grid Margin="15,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="EXTENSIONS" Foreground="#CCCCCC" FontSize="11" FontWeight="Bold" Margin="5,5,0,15" Grid.Row="0"/>

            <ui:TextBox x:Name="SearchBox" Grid.Row="1" PlaceholderText="Search Marketplace..." Margin="0,0,0,10" Foreground="#CCCCCC" KeyDown="SearchBox_KeyDown" />

            <ListView x:Name="ExtensionsListView" Grid.Row="2" Background="Transparent" BorderThickness="0" Foreground="#CCCCCC" ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionChanged="ExtensionsListView_SelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0,5"/>
                        <Setter Property="Margin" Value="0,2"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <ui:SymbolIcon Symbol="PuzzlePiece24" FontSize="32" Grid.Column="0" VerticalAlignment="Top" Margin="0,5,0,0"/>
                            
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#AAAAAA" TextWrapping="Wrap" Margin="0,2,0,5"/>
                                <TextBlock Text="{Binding Author}" FontSize="11" Foreground="#888888" Margin="0,0,0,5"/>
                                
                                <ui:Button Content="{Binding ActionText}" Tag="{Binding Id}" Click="ActionButton_Click" 
                                           HorizontalAlignment="Left" Padding="15,5" 
                                           Appearance="{Binding ButtonAppearance}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</Page>
