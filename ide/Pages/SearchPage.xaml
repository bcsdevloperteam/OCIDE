<Page x:Class="OCIDE.Pages.SearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="SearchPage">

    <Border Background="#252526" Width="280" HorizontalAlignment="Left" CornerRadius="8">
        <Grid Margin="15,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="SEARCH" Foreground="#CCCCCC" FontSize="11" FontWeight="Bold" Margin="5,5,0,15" Grid.Row="0"/>

            <ui:TextBox x:Name="SearchBox" Grid.Row="1" PlaceholderText="Search" Margin="0,0,0,10" Foreground="#CCCCCC" KeyDown="SearchBox_KeyDown" />
            <CheckBox x:Name="MatchCaseCheckBox" Grid.Row="2" Content="Match Case" Foreground="#CCCCCC" Margin="0,0,0,10" />

            <TreeView x:Name="ResultsTreeView" Grid.Row="3" Background="Transparent" BorderThickness="0" Foreground="#CCCCCC" SelectedItemChanged="ResultsTreeView_SelectedItemChanged">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                        <Setter Property="IsExpanded" Value="True"/>
                        <Setter Property="Padding" Value="4"/>
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Matches}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="{Binding Icon}" Margin="0,0,8,0" FontSize="16" Foreground="#CCCCCC"/>
                            <TextBlock Text="{Binding Header}" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Grid>
    </Border>
</Page>
